# OpenApi example with Angular frontend serverless backend

## Instal dependecies:
- The application using [Node.js](https://nodejs.org/en/download)
- in frontend folder run ``npm install``
- in backend folder run ``npm install``
